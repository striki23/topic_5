# С помощью функции set() записываем уникальные значения введенной пользователем строки
line = set(input('Введите строку: '))

# Выводим результат, перед этим конвертируем множество в список
print(list(line))